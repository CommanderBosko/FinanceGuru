__pycache__/
*.pyc
*.pyo
.pytest_cache/
dist/
build/
*.egg-info/
result
result-*

# Local Flatpak build/test artifacts (flatpak-builder, exported repo, bundle)
.flatpak-builder/
build-dir/
repo/
*.flatpak

# Never commit the financial database (lives in ~/.local/share by default,
# but guard against accidental copies into the tree)
*.db
*.sqlite
*.sqlite3
